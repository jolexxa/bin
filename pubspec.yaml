name: bin
description: Typed binary data structures for Dart (without code generation).
version: 0.1.0
repository: https://github.com/jolexxa/bin
topics:
  - binary
  - serialization

environment:
  sdk: ^3.11.0

dependencies:
  binarize: ^2.0.0

dev_dependencies:
  mocktail: ^1.0.5
  test: ^1.31.1
  test_coverage_badge: ^0.3.2
  very_good_analysis: ^10.2.0
